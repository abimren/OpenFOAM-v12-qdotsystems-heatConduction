/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  12
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    format      ascii;
    class       volScalarField;
    object      T;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 0 0 1 0 0 0];

internalField   uniform 20;

boundaryField
{
    leftWall
    {
        type            codedMixed;
        refValue        uniform 0;
        refGradient     uniform 0;
        valueFraction   uniform 1;

        name    convectionLeftWall;

        code
        #{
            const scalarField& delta = patch().deltaCoeffs();
            const scalar h=150.0;
            const scalar Tinf=20.0;
            const scalar k=3.0;
            const scalar q=-1000.0;
            this->refValue() = Tinf;
            this->refGrad() = q/k;
            this->valueFraction() = 1.0/(1.0+k/(h*(1.0/delta)));
        #};
    }

    rightWall
    {
        type            codedMixed;
        refValue        uniform 0;
        refGradient     uniform 0;
        valueFraction   uniform 1;

        name    convectionRightWall;

        code
        #{
            const scalarField& delta = patch().deltaCoeffs();
            const scalar h=50.0;
            const scalar Tinf=80.0;
            const scalar k=3.0;
            const scalar q=-100.0;
            this->refValue() = Tinf;
            this->refGrad() = q/k;
            this->valueFraction() = 1.0/(1.0+k/(h*(1.0/delta)));
        #};
    }

    otherWalls
    {
        type            	empty;
    }
}

// ************************************************************************* //
